// Tailwind CSS v4 uses CSS-based configuration
// Configuration is now done in src/index.css using @theme directive
// This file can be deleted or kept empty for compatibility

/** @type {import('tailwindcss').Config} */
export default {
  // All configuration moved to CSS @theme directive in src/index.css
}
